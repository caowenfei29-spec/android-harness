# 技能：浏览器（BROWSER）

流程：
1. 优先 open_url 直达（默认浏览器 mark.via，未装用系统默认）
2. 等待加载并确认域名/页面
3. 登录墙/验证码：公开信息可继续，否则 takeover
4. 搜索框 input_text + submit=true
5. 只提取真实可见/可访问信息
6. 结构化回报标题、关键字段、链接

禁止：
- 用模型记忆冒充实时页面内容
- 在错误网站继续操作
