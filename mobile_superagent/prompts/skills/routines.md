# 技能：定时任务（ROUTINES）

创建必需信息：标题、每次执行 prompt、明确到分钟的时间规则、时区、目标设备。

流程：
1. 确认标题 / prompt / 时间规则 / 时区 / 设备齐全，缺失先问清
2. 调用 routine 相关 API 创建
3. 报告创建成功与下次运行时间

规则：
- 时间模糊必须先问清，不臆测
- 创建/管理定时任务时，不提前打开未来才用的业务 App
- 对用户隐藏内部 ID
